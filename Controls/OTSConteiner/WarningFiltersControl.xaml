<UserControl x:Class="Kas.WarningFiltersControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 mc:Ignorable="d">

	<UserControl.Resources>
		<!-- Стиль для бордера предупреждения -->
		<Style x:Key="WarningBorderStyle" TargetType="Border">
			<EventSetter Event="MouseUp" Handler="Border_MouseUp"/>
			<Setter Property="Visibility" Value="Collapsed"/>
			<Setter Property="BorderBrush" Value="LightYellow"/>
			<Setter Property="BorderThickness" Value="1"/>
			<Setter Property="CornerRadius" Value="4"/>
			<Setter Property="Padding" Value="8,4"/>
		</Style>

		<!-- Стиль для StackPanel внутри бордера -->
		<Style x:Key="WarningStackStyle" TargetType="StackPanel">
			<Setter Property="Orientation" Value="Horizontal"/>
			<Setter Property="Cursor" Value="Hand"/>
		</Style>

		<!-- Стиль для иконки ️ -->
		<Style x:Key="WarningIconStyle" TargetType="TextBlock">
			<Setter Property="Text" Value="⚠️"/>
			<Setter Property="FontSize" Value="14"/>
			<Setter Property="VerticalAlignment" Value="Center"/>
			<Setter Property="Foreground" Value="LightYellow"/>
		</Style>

		<!-- Стиль для текста предупреждения -->
		<Style x:Key="WarningTextStyle" TargetType="TextBlock">
			<Setter Property="FontWeight" Value="Bold"/>
			<Setter Property="Foreground" Value="LightYellow"/>
			<Setter Property="Margin" Value="4,0,0,0"/>
			<Setter Property="VerticalAlignment" Value="Center"/>
		</Style>
	</UserControl.Resources>

	<WrapPanel Orientation="Horizontal">

		<Border x:Name="EmptyFieldsBorder" Background="DarkOrange" Style="{StaticResource WarningBorderStyle}" d:Visibility="Visible">
			
			<StackPanel Style="{StaticResource WarningStackStyle}">
				<TextBlock Style="{StaticResource WarningIconStyle}"/>
				<TextBlock x:Name="EmptyFieldsLabel" Text="Незаполненные поля" Style="{StaticResource WarningTextStyle}"/>
			</StackPanel>
		</Border>

		<Border x:Name="VidTyagiBorder" Background="#FF4E00" Style="{StaticResource WarningBorderStyle}" d:Visibility="Visible">
			<StackPanel Style="{StaticResource WarningStackStyle}">
				<TextBlock Style="{StaticResource WarningIconStyle}"/>
				<TextBlock x:Name="VidTyagiLabel" Text="Не указан вид тяги" Style="{StaticResource WarningTextStyle}"/>
			</StackPanel>
		</Border>

		<Border x:Name="IsVioletsBorder" Background="DarkViolet" Style="{StaticResource WarningBorderStyle}" d:Visibility="Visible">
			<StackPanel Style="{StaticResource WarningStackStyle}">
				<TextBlock Style="{StaticResource WarningIconStyle}"/>
				<TextBlock x:Name="VioletsLabel" Text="Незаполненные описания" Style="{StaticResource WarningTextStyle}"/>
			</StackPanel>
		</Border>

		<Border x:Name="IsRedsMashBrdr" Background="DarkBlue" Style="{StaticResource WarningBorderStyle}" d:Visibility="Visible">
			<StackPanel Style="{StaticResource WarningStackStyle}">
				<TextBlock Style="{StaticResource WarningIconStyle}"/>
				<TextBlock x:Name="IsRedsMashTBlk" Text="Неверно указан машинист" Style="{StaticResource WarningTextStyle}"/>
			</StackPanel>
		</Border>

		<Border x:Name="IsEmptyPCHBrdr" Background="DarkRed" Style="{StaticResource WarningBorderStyle}" d:Visibility="Visible">
			<StackPanel Style="{StaticResource WarningStackStyle}">
				<TextBlock Style="{StaticResource WarningIconStyle}"/>
				<TextBlock x:Name="IsEmptyPCHTBlk" Text="Не указаны потери П/Ч" Style="{StaticResource WarningTextStyle}"/>
			</StackPanel>
		</Border>

	</WrapPanel>

	<!--<WrapPanel Orientation="Horizontal">
		 --><!--xmlns:local="clr-namespace:KACAHT_Next2.Kas"--><!--
		<Border x:Name="EmptyFieldsBorder" Background="DarkOrange" MouseUp="Border_MouseUp" Visibility="Collapsed" BorderBrush="LightYellow" BorderThickness="1" CornerRadius="4" Padding="8,4">
			<StackPanel Orientation="Horizontal" Cursor="Hand">
				<TextBlock Text="⚠️" FontSize="14" VerticalAlignment="Center" Foreground="LightYellow"/>
				<TextBlock x:Name="EmptyFieldsLabel" Text="Незаполненные поля" FontWeight="Bold" Foreground="LightYellow" Margin="4,0,0,0" VerticalAlignment="Center"/>
			</StackPanel>
		</Border>

		<Border x:Name="VidTyagiBorder" Background="#FF4E00" MouseUp="Border_MouseUp" Visibility="Collapsed" BorderBrush="LightYellow" BorderThickness="1" CornerRadius="4" Padding="8,4">
			<StackPanel Orientation="Horizontal" Cursor="Hand">
				<TextBlock Text="⚠️" FontSize="14" VerticalAlignment="Center" Foreground="LightYellow"/>
				<TextBlock x:Name="VidTyagiLabel" Text="Не указан вид тяги" FontWeight="Bold" Foreground="LightYellow" Margin="4,0,0,0" VerticalAlignment="Center"/>
			</StackPanel>
		</Border>

		<Border x:Name="IsVioletsBorder" Background="DarkViolet" MouseUp="Border_MouseUp" Visibility="Collapsed" BorderBrush="LightYellow" BorderThickness="1" CornerRadius="4" Padding="8,4">
			<StackPanel Orientation="Horizontal" Cursor="Hand">
				<TextBlock Text="⚠️" FontSize="14" VerticalAlignment="Center" Foreground="LightYellow"/>
				<TextBlock x:Name="VioletsLabel" Text="Незаполненные описания" FontWeight="Bold" Foreground="LightYellow" Margin="4,0,0,0" VerticalAlignment="Center"/>
			</StackPanel>
		</Border>

		<Border x:Name="IsRedsMashBrdr" Background="DarkBlue" MouseUp="Border_MouseUp" Visibility="Collapsed" BorderBrush="LightYellow" BorderThickness="1" CornerRadius="4" Padding="8,4">
			<StackPanel Orientation="Horizontal" Cursor="Hand">
				<TextBlock Text="⚠️" FontSize="14" VerticalAlignment="Center" Foreground="LightYellow"/>
				<TextBlock x:Name="IsRedsMashTBlk" Text="Неверно указан машинист" FontWeight="Bold" Foreground="LightYellow" Margin="4,0,0,0" VerticalAlignment="Center"/>
			</StackPanel>
		</Border>

		<Border x:Name="IsEmptyPCHBrdr" Background="DarkRed" MouseUp="Border_MouseUp" Visibility="Collapsed" BorderBrush="LightYellow" BorderThickness="1" CornerRadius="4" Padding="8,4">
			<StackPanel Orientation="Horizontal" Cursor="Hand">
				<TextBlock Text="⚠️" FontSize="14" VerticalAlignment="Center" Foreground="LightYellow"/>
				<TextBlock x:Name="IsEmptyPCHTBlk" Text="Не указаны потери П/Ч" FontWeight="Bold" Foreground="LightYellow" Margin="4,0,0,0" VerticalAlignment="Center"/>
			</StackPanel>
		</Border>

	</WrapPanel>-->
</UserControl>
