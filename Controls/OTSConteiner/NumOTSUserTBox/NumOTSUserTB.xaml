<UserControl x:Class="Kas.NumOTSUserTB"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KACAHT_Next2.Kas"
             Background="Transparent"
             mc:Ignorable="d" 
              TextOptions.TextFormattingMode="Display"
             TextOptions.TextRenderingMode="ClearType"
             UseLayoutRounding="True"
             
             d:DesignHeight="30" d:DesignWidth="400">
	<UserControl.Resources>
		<Style x:Key="BaseStyle" TargetType="TextBlock">
			<Setter Property="Height" Value="24"/>
			<Setter Property="Background" Value="Transparent"/>
			<Setter Property="FontSize" Value="16"/>
			<Setter Property="FontWeight" Value="SemiBold"/>
			<Setter Property="TextAlignment" Value="Center"/>
			<Setter Property="VerticalAlignment" Value="Center"/>
			<Setter Property="Foreground" Value="{StaticResource SplitterBrush}"/>
		</Style>
		<Style x:Key="Lupa" TargetType="TextBlock" BasedOn="{StaticResource BaseStyle}">
			<Setter Property="Width" Value="24"/>
			<Setter Property="Margin" Value="1 0"/>
			<Setter Property="Cursor" Value="Hand"/>
			<Style.Triggers>
				<Trigger Property="IsMouseOver" Value="True">
					<Setter Property="Foreground" Value="DarkBlue"/>
				</Trigger>
			</Style.Triggers>
		</Style>
	</UserControl.Resources>

	<Border BorderThickness="1" BorderBrush="{StaticResource SplitterBrush}" CornerRadius="4" Padding="2" Background="Transparent">
		<Grid>
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="Auto"/>
				<ColumnDefinition Width="Auto"/>
				<ColumnDefinition Width="*"/>
			</Grid.ColumnDefinitions>

			<TextBlock x:Name="BtnFilter" 
                       Grid.Column="0"
                       ToolTip="Найти по № ОТС"
					   FontSize="17"
                       Style="{StaticResource Lupa}"
                       Text="🔎"
                       MouseUp="BtnFilter_MouseUp"/>

			<TextBlock x:Name="BtnFndLok"   
                       Grid.Column="1" 
                       ToolTip="Найти по № локомотива" 
					   FontSize="17"
                       Style="{StaticResource Lupa}"
                       Text="🚂"
                       MouseUp="BtnFndLok_MouseUp"/>

			<local:NumOTSEditBox x:Name="EditBox"
                                 Grid.Column="2"
                                 Text="{Binding Text, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"
                                 Count="{Binding Count, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"
                                 Cleared="EditBox_Cleared"
                                 Copied="EditBox_Copied"/>
		</Grid>
	</Border>
</UserControl>
