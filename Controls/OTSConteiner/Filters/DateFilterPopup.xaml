<UserControl x:Class="Kas.DateFilterPopup"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KACAHT_Next2.Kas"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="250">
    <UserControl.Resources>
        <!-- Стиль для TreeViewItem -->
        <HierarchicalDataTemplate x:Key="DateTreeNodeTemplate" 
                                  ItemsSource="{Binding Children}">
            <CheckBox Content="{Binding Header}" 
                      IsChecked="{Binding IsChecked, Mode=TwoWay}" 
                      IsThreeState="True" />
        </HierarchicalDataTemplate>
    </UserControl.Resources>

    <Border Background="{StaticResource AppBackBrush}"
            BorderBrush="{StaticResource SplitterBrush}"
            BorderThickness="2"
            CornerRadius="8"
            Padding="1">
        <StackPanel Margin="2">
            <TreeView x:Name="DateTreeView"
                      Background="{StaticResource AppBackBrush}"
                      BorderThickness="0"
                      MaxHeight="250"
                      Margin="2"
                      ItemTemplate="{StaticResource DateTreeNodeTemplate}">
                <!-- Подключаем шаблон -->
                <TreeView.Resources>
                    <!-- 👇 Подключаем ваш уже существующий стиль -->
                    <Style TargetType="{x:Type ScrollBar}" BasedOn="{StaticResource CustomScrollBarStyle}" />
                </TreeView.Resources>
            </TreeView>
            <Button x:Name="ApplyButton"
                        Style="{StaticResource PopupStyleButton}"
                        Content="Применить"
                        Margin="2"
                        
                        Click="ApplyButton_Click"/>
        </StackPanel>
    </Border>
</UserControl>
