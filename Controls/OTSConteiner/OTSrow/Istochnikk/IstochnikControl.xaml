<UserControl x:Class="Kas.IstochnikControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KACAHT_Next2.Kas" 
             mc:Ignorable="d" 
             Focusable="True"
             Width="450"       
             Loaded="UserControl_Loaded">

    <Border Background="{StaticResource AppBackBrush}"
            CornerRadius="6"
            BorderBrush="{StaticResource SplitterBrush}"
            BorderThickness="6">
        
            <!-- Main content area -->
            <Grid x:Name="MainGrid">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <!--<RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>-->
                </Grid.RowDefinitions>

                <!-- Заголовок -->
                <Border Grid.Row="0"  Padding="10,5" CornerRadius="3,3,0,0">
                <!--<TextBlock Text="{Binding MestoOTS, StringFormat='Укажите источник отказа \{0\}', 
                 FallbackValue='Укажите источник отказа: ***'}"
                              FontWeight="Bold" HorizontalAlignment="Center"  TextWrapping="Wrap" TextAlignment="Justify"
                              FontSize="17"/>-->
                <StackPanel HorizontalAlignment="Center" Margin=" 0 0 0 5">
                    <TextBlock Text="Укажите источник для отказа" 
                               Foreground="Black" FontWeight="DemiBold" 
                               TextAlignment="Center" FontSize="17"/>
                    <TextBlock Text="{Binding MestoOTS, FallbackValue='***'}"
                               Foreground="DarkRed" FontWeight="Bold" 
                               TextAlignment="Center" FontSize="17" 
                               TextWrapping="Wrap"/>
                </StackPanel>


            </Border>

                <ScrollViewer Grid.Row="1" >
                    <ScrollViewer.Resources>
                        <!-- 👇 Подключаем ваш уже существующий стиль -->
                        <Style TargetType="{x:Type ScrollBar}" BasedOn="{StaticResource CustomScrollBarStyle}" />
                    </ScrollViewer.Resources>
                    <WrapPanel x:Name="WPan" HorizontalAlignment="Center" VerticalAlignment="Top" PreviewKeyDown="WPan_PreviewKeyDown">
                        
                    </WrapPanel>
                </ScrollViewer>
                <!-- Row 6: OK/Отмена -->
                <Border Grid.Row="2"  Padding="2" Margin="2 6">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <!-- Кнопка OK -->
                        <Button x:Name="OkButton" Content="OK" 
                    Style="{StaticResource PopupStyleButton}" 
                    Click="OkButton_Click" 
                                IsDefault="True"
                    Margin="2"
                    Width="120"
                    Height="35"
                    Grid.Column="0"
                    IsEnabled="False"
                    HorizontalAlignment="Left"/>

                        <!-- Кнопка Отмена -->
                        <Button Content="Отмена"
                    Style="{StaticResource PopupStyleButton}" 
                    Click="CancelButton_Click" 
                                IsCancel="True"
                    Margin="2"
                    Width="120" 
                    Height="35"
                    Grid.Column="1"
                    HorizontalAlignment="Right"/>
                    </Grid>
                </Border>

            </Grid>

        
        
    </Border>
</UserControl>