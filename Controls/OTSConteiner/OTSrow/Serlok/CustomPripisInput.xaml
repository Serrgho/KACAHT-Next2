<UserControl x:Class="Kas.CustomPripisInput"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KACAHT_Next2.Kas"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Background="{StaticResource AppBackBrush}" 
            BorderBrush="Gray"
            Margin="5"
            BorderThickness="1" 
            CornerRadius="4">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Заголовок -->
            <TextBlock Grid.Row="0" Text="Добавить новую приписку" 
                      FontWeight="Bold" FontSize="14" 
                      HorizontalAlignment="Center" Margin="0,0,0,15"/>

            <!-- Ввод приписки -->
            <StackPanel Grid.Row="1" Orientation="Vertical" Margin="0,0,0,15">
                <TextBlock Text="Приписка локомотива:" Margin="0,0,0,5"
                          FontWeight="SemiBold"/>
                <TextBox x:Name="PripisTextBox" Height="30" FontSize="14"
                        TextChanged="PripisTextBox_TextChanged"
                        Padding="5"/>
            </StackPanel>

            <!-- Дорога -->
            <TextBlock Grid.Row="2" Text="Дорога (группа):" 
                      Margin="0,0,0,5"
                      FontWeight="SemiBold"/>

            <!-- RadioButtons в WrapPanel -->
            <Border Grid.Row="3" BorderBrush="LightGray" BorderThickness="1"
                   CornerRadius="3" Margin="0,0,0,15" Padding="5">
                <ScrollViewer VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Disabled">
                    <WrapPanel x:Name="RadioButtonsPanel" Orientation="Horizontal"/>
                </ScrollViewer>
            </Border>

            <!-- Кнопки -->
            <StackPanel Grid.Row="4" Orientation="Horizontal" 
                       HorizontalAlignment="Right" Margin="2 5">
                <Button x:Name="OkButton" Content="Добавить" Click="OkButton_Click"
                       Style="{StaticResource PopupStyleButton}"
                       Width="100" Height="35" Margin="0,0,10,0"
                       IsDefault="True"  Cursor=""/>
                <Button x:Name="CancelButton" Content="Отмена" Click="CancelButton_Click"
                       Style="{StaticResource PopupStyleButton}"
                       Width="100" Height="35" 
                       IsCancel="True" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
