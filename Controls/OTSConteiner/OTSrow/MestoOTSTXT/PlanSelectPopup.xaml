<UserControl x:Class="Kas.PlanSelectPopup"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KACAHT_Next2.Kas"
             mc:Ignorable="d" 
              d:DesignWidth="220" d:DesignHeight="300"
             Background="Transparent"
             x:Name="Root">
	<UserControl.Resources>
		<!-- Тот же стиль пунктов, что и в CommentCopyPopup -->
		<Style x:Key="PopupItemStyle" TargetType="TextBlock">
			<Setter Property="Padding" Value="6,4"/>
			<Setter Property="Cursor" Value="Hand"/>
			<Setter Property="TextWrapping" Value="Wrap"/>
			<Setter Property="TextAlignment" Value="Left"/>
			<Setter Property="FontWeight" Value="DemiBold"/>
			<Setter Property="VerticalAlignment" Value="Center"/>
			<Setter Property="Background" Value="Transparent"/>
			<Setter Property="MaxWidth" Value="420"/>
			
			<Style.Triggers>
				<Trigger Property="IsMouseOver" Value="True">
					<Setter Property="Background" Value="{StaticResource ScrolBarBrush}"/>
				</Trigger>
			</Style.Triggers>
		</Style>

		<Style x:Key="PopupGreenStyle" TargetType="TextBlock" BasedOn="{StaticResource PopupItemStyle}">
			<Setter Property="Foreground" Value="DarkGreen"/>
		</Style>
	</UserControl.Resources>

	<!-- Placement="MousePoint" — попап откроется прямо в точке правого клика -->
	<Popup x:Name="MainPopup" StaysOpen="False" AllowsTransparency="True"
           Placement="MousePoint" PopupAnimation="Fade">

		<Border Background="{StaticResource AppBackBrush}" CornerRadius="8" 
                BorderBrush="{StaticResource SplitterBrush}" BorderThickness="3" 
                Padding="2" Width="220" MaxHeight="500">
			<Border.Effect>
				<DropShadowEffect BlurRadius="12" ShadowDepth="4" Color="#33000000" Direction="315"/>
			</Border.Effect>

			<Grid>
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto"/>
					<RowDefinition Height="Auto"/>
					<RowDefinition Height="*"/>
				</Grid.RowDefinitions>

				<TextBlock Text="➕ Добавить план:" FontWeight="SemiBold" FontSize="13"
                           Foreground="Blue" Padding="6,4"/>

				<Separator Grid.Row="1"/>

				<ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" 
                              HorizontalScrollBarVisibility="Disabled">
					<ScrollViewer.Resources>
						<Style TargetType="{x:Type ScrollBar}" BasedOn="{StaticResource CustomScrollBarStyle}" />
					</ScrollViewer.Resources>
					<StackPanel x:Name="PlansStack"/>
				</ScrollViewer>
			</Grid>
		</Border>
	</Popup>
</UserControl>
