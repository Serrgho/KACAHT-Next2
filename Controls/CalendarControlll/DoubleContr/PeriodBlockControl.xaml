<UserControl x:Class="Kas.PeriodBlockControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KACAHT_Next2.Kas"
             Loaded="UserControl_Loaded"
             mc:Ignorable="d">
    <Grid Background="{StaticResource AppBackBrush}" Height="auto">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <RadioButton Grid.Column="0" x:Name="But18" HorizontalAlignment="Center" Checked="But18_Checked" Style="{StaticResource PopupStyleRadioButton}" >18:00-18:00</RadioButton>
            <RadioButton Grid.Column="1" x:Name="But23" HorizontalAlignment="Center" Checked="But23_Checked" Style="{StaticResource PopupStyleRadioButton}" >00:00-23:59</RadioButton>
        </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" x:Name="DNach" HorizontalAlignment="Center" FontSize="17" FontWeight="Bold"> --- </TextBlock>
        <TextBlock Grid.Column="1" HorizontalAlignment="Center"/>
        <TextBlock x:Name="DKon" Grid.Column="2" HorizontalAlignment="Center" FontSize="17" FontWeight="Bold"> --- </TextBlock>
        </Grid>
        
        

        
    </Grid>
</UserControl>
