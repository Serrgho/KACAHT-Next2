<UserControl x:Class="Kas.KasJournalParamControl0"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KACAHT_Next2.Kas"
             xmlns:properties="clr-namespace:KACAHT_Next2.My"
             mc:Ignorable="d">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="Auto"/>
			<ColumnDefinition Width="Auto"/>
			<ColumnDefinition Width="*"/>
			<ColumnDefinition Width="Auto"/>
		</Grid.ColumnDefinitions>

		<Button Grid.Column="1" x:Name="btnSetPeriodJournal" Foreground="DarkRed" Content="🛠️Изменить" Style="{StaticResource PopupStyleButton}" Margin="2" Click="btnSetPeriodJournal_Click"  />
		<Button Grid.Column="0"  x:Name="btnReSetPeriodJournal" Content="🔅Сброс" Style="{StaticResource PopupStyleButton}" Margin="2" Click="btnReSetPeriodJournal_Click"  />
		<TextBlock Grid.Column="2" Text="{Binding MWFetcher.NachKon_TXT}" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="DarkRed" Padding="2 0" Margin="10 0"/>
		<!--Style="{StaticResource MyCombo}"-->
		<ComboBox Grid.Column="3" x:Name="DorTBlock"
				Style="{StaticResource MyCombo}"
				VerticalContentAlignment="Center"
				FontSize="15"
				ItemsSource="{x:Static local:KasJournalParamControl0.RailwayCodes}" 
				DisplayMemberPath="Name"	
				SelectedValuePath="Code"	
				SelectedValue="{Binding MWFetcher.DorOfOTS, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
				MinWidth="180" VerticalAlignment="Bottom" Margin="2" />
	</Grid>
</UserControl>
